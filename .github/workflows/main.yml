name: CI

on: [push, pull_request]
jobs:
  build-osx:
    runs-on: macos-latest
    strategy:
      fail-fast: false

    steps:
    - uses: actions/checkout@v4

    - name: brew upgrade
      run: brew upgrade

    - name: install make
      run: brew install make

    - name: install gcc
      run: brew install gcc@14

    - name: make toolchain
      run: sh toolchain.sh

    - name: upload ar
      uses: actions/upload-artifact@v4
      with:
        name: ar
        path: toolchain/binutils-2.37/binutils/ar
  


  
